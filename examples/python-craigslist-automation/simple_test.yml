name: Simple Python Test
description: Basic Python engine test

steps:
  - id: python_hello
    tool_name: run_command
    arguments:
      engine: "python"
      run: |
        log("Hello from Python!")
        return {"message": "Python works!", "value": 42}