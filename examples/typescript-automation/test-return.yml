# Test TypeScript return value capture

steps:
  - tool_name: run_command
    arguments:
      engine: typescript
      run: |
        console.log('Testing TypeScript return value');

        const result = {
          status: 'success',
          message: 'TypeScript returns work!',
          timestamp: new Date().toISOString()
        };

        console.log('About to return:', result);
        return result;