name: Test Module Error Handling
description: Test workflow to verify improved error messages for missing modules

steps:
  - tool_name: run_command
    arguments:
      engine: "javascript"
      run: |
        // This should trigger a module not found error
        const validator = require('./helpers/validator.js');
        console.log('This should not execute');