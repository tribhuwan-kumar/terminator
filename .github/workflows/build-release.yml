name: Build & Release Windows Server

on:
  push:
    tags:
      - 'v*.*.*' # Trigger on version tags like v1.0.0
  workflow_dispatch: # Allow manual triggering

jobs:
  build-release:
    name: Build & Release (Windows)
    runs-on: windows-latest
    permissions:
      contents: write # Needed to create releases

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          target: x86_64-pc-windows-msvc

      - name: Build server (x86_64)
        run: cargo build --verbose --release --package server --target x86_64-pc-windows-msvc
        env:
          CARGO_TERM_COLOR: always

      - name: Build server (ARM)
        run: cargo build --verbose --release --package server --target aarch64-pc-windows-msvc
        env:
          CARGO_TERM_COLOR: always

      - name: Package executable
        shell: pwsh
        run: |
          $x86ExePath = "target/x86_64-pc-windows-msvc/release/server.exe"
          $armExePath = "target/aarch64-pc-windows-msvc/release/server.exe"
          $x86ZipPath = "terminator-server-windows-x86_64.zip"
          $armZipPath = "terminator-server-windows-arm64.zip"
          Compress-Archive -Path $x86ExePath -DestinationPath $x86ZipPath
          Compress-Archive -Path $armExePath -DestinationPath $armZipPath
          echo "ASSET_PATH_X86=$x86ZipPath" >> $env:GITHUB_ENV
          echo "ASSET_NAME_X86=$x86ZipPath" >> $env:GITHUB_ENV
          echo "ASSET_PATH_ARM=$armZipPath" >> $env:GITHUB_ENV
          echo "ASSET_NAME_ARM=$armZipPath" >> $env:GITHUB_ENV

      - name: Upload Artifacts (Manual Trigger)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ASSET_NAME_X86 }}
          path: ${{ env.ASSET_PATH_X86 }}
      - name: Upload Artifacts (Manual Trigger - ARM)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ASSET_NAME_ARM }}
          path: ${{ env.ASSET_PATH_ARM }}

      - name: Create Release and Upload Assets (Tag Trigger)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ env.ASSET_PATH_X86 }}
            ${{ env.ASSET_PATH_ARM }}
          name: Release ${{ github.ref_name }}
          generate_release_notes: true

